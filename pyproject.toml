[project]
name = "number-placement-game"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandas>=2.2.3",
    "plotly>=6.0.0",
]

[tool.uv]
dev-dependencies = [
    "ruff>=0.9.5",
]
